# CP-templates

## Sparse Table

- Supports range queries on static arrays
- Overlap query: O(1) (min / max / gcd)
- Non-overlap query: O(log n) (any associative operation)

Tested on Codeforces problems.

